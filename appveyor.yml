version: 0.0.{build}

# branches to build
branches:
  # whitelist
  only:
    - master

# Test against the latest version of this Node.js version
environment:
  nodejs_version: "Current"

# Install scripts. (runs after repo cloning)
install:
  # Get the latest stable version of Node.js or io.js
  - ps: Install-Product node $env:nodejs_version x64
  # install modules
  - npm install

# Post-install test scripts.
test_script:
  # Output useful info for debugging.
  - node --version
  - npm --version
  # run tests
  # - npm test

after_test:
  - ps: |
      set npm_config_target=1.7.10
      set npm_config_arch=x64
      set npm_config_target_arch=x64
      set npm_config_disturl=https://atom.io/download/electron
      set npm_config_runtime=electron
      set npm_config_build_from_source=true
      npm install --no-package-lock
      msbuild 'scripts/lib/windows/Zeo VR.sln'
      cp 'scripts/lib/windows/Zeo VR/bin/Zeo VR.exe' .
      cp 'scripts/lib/windows/Zeo VR Server/bin/Zeo VR Server.exe' server.exe
      cp 'scripts/lib/windows/Zeo VR Connect/bin/Zeo VR Connect.exe' connect.exe
      cp 'scripts/lib/windows/Zeo VR Server Reset/bin/Zeo VR Server Reset.exe' reset.exe
      7z a windows-release.zip * -xr'!.git'

artifacts:
  - path: "windows-release.zip"
    name: windows-release

# Don't actually build.
build: off
